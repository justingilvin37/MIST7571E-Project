I used AI tools to help translate our project idea into a focused client-side application and to review the flow of the JavaScript. AI helped identify places where the app needed explicit loading, validation, and error states, especially around the OpenAI request and invalid numeric inputs. I also used AI to refine the prompt structure so the generated brief stays cautious: it must use the supplied project inputs, avoid inventing permits or local contamination facts, and distinguish estimates from verified information. I reviewed and edited the output structure, transparent screening logic, and wording so the tool does not present itself as an engineering or legal analysis. The OpenAI key is entered at runtime and is not hardcoded in the source files.
